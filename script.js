let map;function findMe(){if(!navigator.geolocation){alert('Geolocation wird nicht unterstützt.');return;}navigator.geolocation.getCurrentPosition(success,error);}function success(position){const lat=position.coords.latitude;const lon=position.coords.longitude;if(!map){map=L.map('map').setView([lat,lon],15);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'&copy; OpenStreetMap'}).addTo(map);}L.marker([lat,lon]).addTo(map).bindPopup('Dein Standort').openPopup();document.getElementById('info').innerHTML=`<h2>Standort gefunden</h2><p>Breitengrad: ${lat}</p><p>Längengrad: ${lon}</p>`;}function error(){alert('Standort konnte nicht ermittelt werden.');}